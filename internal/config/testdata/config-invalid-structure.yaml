port: "8080"
environment: "development"
database:
  driver: "sqlite"
  # This will cause unmarshal to fail because port should be a string but we'll make it an object
  port:
    invalid: "structure"
    nested: "object"
